//All files are adjusted and derived from Professor Vanegas's original source code
void setup () {
  size(400, 400);
}

void draw () {
  background(25);
  drawLeafette();
  drawCreature();
  drawBluebee();
  drawMaple();
  drawMint();
  drawMaryjane();
}
